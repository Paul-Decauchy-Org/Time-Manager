sonar.projectKey=Paul-Decauchy-Org_Time-Manager
sonar.organization=paul-decauchy-org

# Sources combinées (backend + frontend)
sonar.sources=back/services,front/app,front/components,front/hooks,front/contexts,front/apollo

# Exclusions globales
sonar.exclusions=**/node_modules/**,**/vendor/**,**/*.spec.ts,**/*.spec.tsx,**/*.test.ts,**/*.test.tsx,back/internal/graph/generated.go,back/internal/graph/schema.graphqls,**/*_test.go,**/generated/**,**/*.config.*,**/dist/**,**/.next/**

# Tests (backend + frontend)
sonar.tests=back/services,front/__tests__
sonar.test.inclusions=**/*_test.go,**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx

# Configuration Go (backend)
sonar.go.coverage.reportPaths=back/coverage

# Configuration JavaScript/TypeScript (frontend)
sonar.javascript.lcov.reportPaths=front/coverage/lcov.info

# Encodage
sonar.sourceEncoding=UTF-8

# Exclusions de coverage (backend + frontend)
sonar.coverage.exclusions=back/internal/graph/**,back/internal/mappers/**,back/internal/models/**,back/package/**,back/cmd/**,back/internal/repositories/**,front/generated/**,front/__tests__/**